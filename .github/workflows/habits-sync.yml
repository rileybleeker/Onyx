name: Habits Sync

on:
  schedule:
    # Every hour at :45
    - cron: '45 * * * *'
  workflow_dispatch: # Manual trigger

concurrency:
  group: habits-sync
  cancel-in-progress: false

jobs:
  habits-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call habits sync endpoint
        env:
          ONYX_CI_TOKEN: ${{ secrets.ONYX_CI_TOKEN }}
        run: |
          curl -sS -X POST \
            -H "Content-Type: application/json" \
            -H "x-onyx-ci-token: $ONYX_CI_TOKEN" \
            --fail-with-body \
            "https://frontend-six-alpha-69.vercel.app/api/habits/sync"
